export declare class AsyncQueue {
    private indexed;
    private waitable;
    private clearWaitTime;
    private index;
    private isProcessing;
    private currentPromiseProcessing;
    private isWaiting;
    private queue;
    constructor(settings?: {
        indexed?: boolean;
        index?: string;
        waitable?: boolean;
        clearWaitTime?: number;
    });
    private processQueue;
    enqueue(promiseFunction: (index?: string) => Promise<any>): void;
    clearQueue(): Promise<void>;
    waitForProcessing(): Promise<true | undefined>;
}
